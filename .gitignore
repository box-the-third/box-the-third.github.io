# ── Sensitive config (generated at deploy time by GitHub Actions) ──
# Locally: copy config.example.js → config.js and fill in your values.
# On GitHub Pages: the Actions workflow generates this from a secret.
# NEVER commit config.js
config.js

# ── OS / editor noise ────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Dependency folders ───────────────────────────────────────
node_modules/
